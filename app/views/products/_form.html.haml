= simple_form_for(product, url: category_products_path(category)) do |f|
  = f.error_notification
  .form-inputs
    = f.input :title
    = f.input :description
    = f.input :price
  .form-actions
    = f.button :submit, "Save", class: 'btn btn-primary'