%h1 New review
%ol.breadcrumb
	%li= link_to 'CATEGORIES', root_path
	%li= link_to category.name, category_path(category)
	%li= link_to 'PRODUCTS', category_products_path(category)
	%li= link_to product.title, category_product_path(category, product)
	%li.active NEW REVIEW

= render 'form'

.buttons
	%span= link_to 'Back', category_product_path(category, product), class: 'btn btn-primary'