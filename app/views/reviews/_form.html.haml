= simple_form_for [category, product, review] do |f|
  = f.error_notification
  .form-inputs
    = f.input :content
    = f.input :rating, collectio: 1..5
  .form-actions
    = f.button :submit, "Save", class: 'btn btn-primary'